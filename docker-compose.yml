services:
  feedflow:
    build: .
    ports:
      - "${FLASK_PORT:-5050}:5050"
    volumes:
      - feed_data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  feed_data:
